# GPU override — use with: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
services:
  watchai-server:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
